extends layout

block content
  p F12를 눌러 console 탭과 network 탭을 확인.
  script.
    const webSocket = new WebSocket('ws://localhost:8005');
    webSocket.onopen = () => {
        console.log('서버와 웹 소켓 연결 성공!');
    };
    webSocket.onmessage = (event) => {
        console.log(event.data);
        webSocket.send('클라이언트에서 서버로 답장을 보냄.');
    };